---
description: Count characters in the provided text
argument-hint: "<text>"
---

Run this command to count characters:

```bash
echo -n "$ARGUMENTS" | wc -c
```

Report the result as: `X characters`